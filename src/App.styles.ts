import { type SxProps } from '@mui/system';

const styles: Record<string, SxProps> = {
    appContainer: {
        paddingLeft: {
            xs: 0,
            md: 2,
        },
        paddingRight: {
            xs: 0,
            md: 2,
        },
    },
};

export default styles;
